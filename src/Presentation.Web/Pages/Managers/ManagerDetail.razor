@page "/managers/details/{id:guid}"
@inject ManagerState managerState

<PersonalInfoCardTemplate
    PersonalInfo="managerState.Current.PersonalInfo"
    Title="Manager details"
    GoBackPath="managers">
    <CardActions>
        <CreateTeamtBtnComponent Manager="managerState.Current" />
    </CardActions>
</PersonalInfoCardTemplate>

@code {
    [Parameter] public Guid id { get; set; }
    protected override async Task OnParametersSetAsync()
    {
        if (managerState.Current is null)
        {
            managerState.SetCurrent(id);
        }
        await base.OnParametersSetAsync();
    }
}
