@page "/players/details/{id:guid}"
@inject PlayerState playerState

<PersonalInfoCardTemplate
    PersonalInfo="playerState.Current.PersonalInfo"
    Title="Player details"
    GoBackPath="players">
    <MoreInfo>
        <MudText><strong>Weight: </strong>@playerState.Current.PersonalInfo.Weight</MudText>
        <MudText><strong>Height: </strong>@playerState.Current.PersonalInfo.Height</MudText>
    </MoreInfo>
</PersonalInfoCardTemplate>

@code {
    [Parameter] public Guid id { get; set; }
    protected override async Task OnParametersSetAsync()
    {
        if (playerState.Current is null)
        {
            playerState.SetCurrent(id);
        }
        await base.OnParametersSetAsync();
    }
}
