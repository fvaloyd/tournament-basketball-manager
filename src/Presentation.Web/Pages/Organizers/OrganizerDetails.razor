@page "/organizers/details/{id:guid}"
@inject OrganizerState organizerState

<PersonalInfoCardTemplate
    PersonalInfo="organizerState.Current.PersonalInfo"
    Title="Organizer details"
    GoBackPath="organizers">
    <CardActions>
        <CreateTournamentBtnComponent Organizer="organizerState.Current" />
    </CardActions>
</PersonalInfoCardTemplate>

@code {
    [Parameter] public Guid id { get; set; }
    protected override Task OnParametersSetAsync()
    {
        if (organizerState.Current is null)
        {
            organizerState.SetCurrent(id);
        }
        return base.OnParametersSetAsync();
    }
}